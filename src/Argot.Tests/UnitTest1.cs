using Argot.XMPP.Core;
using DnsClient;

namespace Argot.Tests
{
    public class UnitTest1
    {
        [Fact]
        public async void Test1()
        {
            var lookupClient = new LookupClient();
//            var settingsService = new(Preferences.Default, SecureStorage.Default);
 //           var client = new XMPPTCPConnection("thing", lookupClient,);
  //          await client.ConnectAsync(jid);
        }
    }
}